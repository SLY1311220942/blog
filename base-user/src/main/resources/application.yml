#========================================spring config=====================================  
spring:
  profiles:
    active: dev
  application:
    name: base-user
  devtools:
    remote:
      restart:
        enabled: false
#========================================mybatis config=====================================
mybatis:
  type-aliases-package: com.sly.blog.base.user.model
  mapper-locations: classpath:mapper/*.xml
