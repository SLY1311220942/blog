#========================================spring config=====================================  
spring:
  profiles:
    active: dev
  application:
    name: base-common
  devtools:
    remote:
      restart:
        enabled: false
#========================================mybatis config=====================================
mybatis:
  type-aliases-package: com.sly.blog.base.common.model
  mapper-locations: classpath:mapper/*.xml
